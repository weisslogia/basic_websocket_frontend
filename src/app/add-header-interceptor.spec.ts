import { AddHeaderInterceptor } from './add-header-interceptor';

describe('AddHeaderInterceptor', () => {
  it('should create an instance', () => {
    expect(new AddHeaderInterceptor()).toBeTruthy();
  });
});
